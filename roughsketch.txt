init
	set adc config
		is 8 bits of precision enough? or 10 necessary? dictates data reading method
		left/right alignment- ADLAR 
		set reference voltage- either AREF on pin ?? or AVcc. requires capacitor on AREF pin
		adc control register ADCSRA
			ADEN (adc enable) to high


timing control



read data
	for i in 0-15:
		update SEL outputs to iterate through external mux outputs
		for j in 0-4 (0-3???):
			update internal mux select to iterate through adc inputs 0-4
				MUX4..0 values for pins 3-7:
				0b00011
				...
				0b00111

				set ADSC on ADCSRA to 1 to begin conversion
				



send CAN message